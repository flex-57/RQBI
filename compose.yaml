services:
  php:
    build: .
    ports: ["8000:8000"]
    volumes: [".:/app"]
    depends_on: [db]
  db:
    image: mariadb:10.8
    environment: { MARIADB_ROOT_PASSWORD: root, MARIADB_DATABASE: cms }
    ports: ["3306:3306"]
